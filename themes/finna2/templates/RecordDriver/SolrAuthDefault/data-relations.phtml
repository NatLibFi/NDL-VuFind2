<?php
  if ($data) {
    $helper = $this->record($this->driver);
  }
?>
<?php foreach ($data as $relation): ?>
  <?=$helper->getAuthorityLinkElement('author', $relation['name'], $relation, ['authorityType' => $relation['type'] ?? null, 'additional' => $helper->getAuthorityLinkAdditionalData(['date', 'role']), 'linkType' => 'author-page'])?><br>
<?php endforeach; ?>
