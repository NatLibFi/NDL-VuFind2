<?php // No ending line feed
if (!$this->id) {
    return;
}
echo $this->url(
    $this->searchAction ?: $this->searchOptions($this->driver->getSourceIdentifier())->getSearchAction(),
    [],
    ['query' => ['filter[]' => sprintf('%s:("%s")', \Finna\Search\Solr\AuthorityHelper::AUTHOR2_ID_FACET, $this->id)]]
);