<!-- START of: finna - myresearch/FinnaResourceList.phtml -->
<?php
  $title = $this->transEsc('FinnaResourceList::Research Room Reservations');
  // Set up page title:
  $this->headTitle($title);

  // Set up breadcrumbs:
  $this->layout()->breadcrumbs = '<li><a href="' . $this->url('FinnaResourceList-home') . '">' . $title . '</a></li> <li class="active">' . $this->transEsc('FinnaResourceList::Reservation List') . '</li>';
?>

<!-- Leftside navigationbar -->
<?= $this->context($this)->renderInContext('myresearch/menu.phtml', ['active' => 'FinnaResourceList']); ?>


<div class="<?=$this->layoutClass('mainbody-myresearch')?> reservation-list-body">
  <h2><?=$title ?></h2>
  <?= $this->flashmessages() ?>
  <ul class="reservations-list">
    <?php foreach ($this->lists as $list): ?>
      <li>
        <a href="<?= $this->url('FinnaResourceList-list', ['id' => $list['id']])?>"><?= $this->escapeHtml($list['title']) ?></a>
        <span class="list-info pull-right">
          <?php if ($list['created_formatted'] ?? false): ?><span class="list-created"><?= $this->transEsc('FinnaResourceList::list_created', ['%%date%%' => $list['created_formatted']])?></span><?php endif; ?>
          <?php if ($list['ordered_formatted'] ?? false): ?><span class="list-ordered"><?= $this->transEsc('FinnaResourceList::list_ordered', ['%%date%%' => $list['ordered_formatted']])?></span><?php endif; ?>
          <a href="<?= $this->url('FinnaResourceList-delete', ['id' => $list['id']])?>" data-lightbox=""><?= $this->transEsc('FinnaResourceList::Delete List') ?></a>
        </span>
      </li>
      <?php endforeach; ?>
  </ul>
</div>
<!-- END of: finna - myresearch/FinnaResourceList.phtml -->
