<? //Don't add START and END comments ?>
<? if (!empty($data)): ?>
  <div class="truncate-field">
    <? foreach($data as $row => $key): ?>
        <? if ($row): ?><br/><? endif ;?><?=implode(', ', $key); ?>
    <? endforeach; ?>
  </div>
<? endif; ?>
