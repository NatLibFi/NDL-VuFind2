<? //Don't add START and END comments ?>
<? if (!empty($data)): ?>
  <div class="truncate-field">
    <? foreach($data as $row): ?>
        <?=implode(', ', $row); ?><br/>
    <? endforeach; ?>
  </div>
<? endif; ?>
